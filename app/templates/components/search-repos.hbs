<form {{action "submit" on="submit"}} class="ui input center aligned grid">
  {{input value=search class="prompt" type="text" placeholder="Enter a User Name"}}
  <button class="ui primary button"{{action "submit"}}>Search</button>
</form>
