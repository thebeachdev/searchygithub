import ApplicationAdapter from './application';

export default ApplicationAdapter.extend({
  //TODO: call properties from parent class.
  // findRecord(store, type, snapshot){
  //   const url = '${this.host}/${this.namespace}/${snapshot.attr('login')}';// make this get ppopulate from the app elsewhere.
  //   return this.ajax(url, "GET", {});
  // }
});
