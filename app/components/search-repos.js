import Ember from 'ember';

export default Ember.Component.extend({
  classNames: ['ui', 'input', 'center', 'aligned', 'grid'],
  // the component should get the value of the search bar and send it to the route.  
});
